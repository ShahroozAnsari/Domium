<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFramework>net10.0</TargetFramework>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>
	  <LangVersion>latest</LangVersion>
    <Description>Concrete DDD primitives for Domium including aggregate roots, entities, value objects, aggregate identifiers, and domain events.</Description>
  </PropertyGroup>

  <ItemGroup>
    <ProjectReference Include="..\Domium.Domain.Abstractions\Domium.Domain.Abstractions.csproj" />
  </ItemGroup>

</Project>
